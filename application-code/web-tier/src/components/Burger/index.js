// Import the default export from the './Burger' component file and re-export it.
// This allows users to import the component from the main folder without having
// to explicitly specify the component file's name.
export { default } from './Burger';
